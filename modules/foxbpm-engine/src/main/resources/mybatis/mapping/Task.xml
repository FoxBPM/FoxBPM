<?xml version="1.0" encoding="UTF-8" ?> 

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
  
<mapper namespace="com.founder.fix.fixflow.core.impl.task.TaskInstanceEntity">

  <!-- TASK INSERT -->
  
  <insert id="insertTaskInstance" parameterType="com.founder.fix.fixflow.core.impl.task.TaskInstanceEntity">
  	insert into FIXFLOW_RUN_TASKINSTANCE(
  	  TASKINSTANCE_ID,
  	  PROCESSINSTANCE_ID, 
  	  PROCESSDEFINITION_ID, 
  	  VERSION,
	  TOKEN_ID,
	  NODE_ID,
	  DESCRIPTION,
	  PARENTTASK_ID,
	  ASSIGNEE,
	  CLAIM_TIME,
	  NAME,
	  CREATE_TIME,
	  START_TIME,
	  ISBLOCKING,
	  END_TIME,
	  DUEDATE,
	  PRIORITY,
	  CATEGORY,
	  OWNER,
	  DELEGATIONSTATESTRING,
	  BIZKEY,
	  COMMAND_TYPE,
	  COMMAND_MESSAGE,
	  TASK_COMMENT,
	  NODE_NAME,
	  PROCESSDEFINITION_KEY,
	  FORMURI,
	  TASKGROUP,
	  TASKTYPE,
	  PROCESSDEFINITION_NAME,
	  ISCANCELLED,
	  ISSUSPENDED,
	  ISOPEN,
	  ISDRAFT,
	  EXPECTED_EXECUTIONTIME,
	  AGENT,
	  ADMIN,
	  FORMURIVIEW,
	  CALLACTIVITY_INSTANCE_ID,
	  COMMAND_ID,
	  PENDINGTASKID
	  ) values(
  	#{id, jdbcType=VARCHAR},
  	#{processInstanceId, jdbcType=VARCHAR},
  	#{processDefinitionId, jdbcType=VARCHAR},
  	#{version, jdbcType=VARCHAR},
  	#{tokenId, jdbcType=VARCHAR},
  	#{nodeId, jdbcType=VARCHAR},
  	#{description, jdbcType=VARCHAR},
  	#{parentTaskInstanceId, jdbcType=VARCHAR},
  	#{assignee, jdbcType=VARCHAR},
  	#{claimTime, jdbcType=TIMESTAMP},
  	#{name, jdbcType=VARCHAR},
  	#{createTime, jdbcType=TIMESTAMP},
  	#{startTime, jdbcType=TIMESTAMP},
  	#{isBlockingString, jdbcType=VARCHAR},
  	#{endTime, jdbcType=TIMESTAMP},
  	#{dueDate, jdbcType=VARCHAR},
  	#{priority, jdbcType=VARCHAR},
  	#{category, jdbcType=VARCHAR},
  	#{owner, jdbcType=VARCHAR},
  	#{delegationStateString, jdbcType=VARCHAR},
  	#{bizKey, jdbcType=VARCHAR},
  	#{commandType, jdbcType=VARCHAR},
  	#{commandMessage, jdbcType=VARCHAR},
  	#{taskComment, jdbcType=VARCHAR},
  	#{nodeName, jdbcType=VARCHAR},
  	#{processDefinitionKey, jdbcType=VARCHAR},
  	#{formUri, jdbcType=VARCHAR},
  	#{taskGroup, jdbcType=VARCHAR},
  	#{taskInstanceTypeString, jdbcType=VARCHAR},
  	#{processDefinitionName, jdbcType=VARCHAR},
  	#{isCancelledString, jdbcType=VARCHAR},
  	#{isSuspendedString, jdbcType=VARCHAR},
  	#{isOpenString, jdbcType=VARCHAR},
  	#{isDraftString, jdbcType=VARCHAR},
  	#{expectedExecutionTime, jdbcType=VARCHAR},
  	#{agent, jdbcType=VARCHAR},
  	#{admin, jdbcType=VARCHAR},
  	#{formUriView, jdbcType=VARCHAR},
  	#{callActivityInstanceId, jdbcType=VARCHAR},
  	#{commandId, jdbcType=VARCHAR},
  	#{pendingTaskId, jdbcType=VARCHAR}
  )
  </insert>

</mapper>